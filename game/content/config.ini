[server]
    storage = database

[building]
    #items_file = game/content/items.json
    items_file = webapp/public/js/items.json
    resources = gold,pop
    # list of resources (consumable | constraint)
    gold = consumable
    pop = consumable

[geomap]
    preload = no
    connfile = game/maps/{}.json
    mapfile = webapp/public/geojson/{}.geojson

[auth]
[turns]

[worlds]
    max_players = 6
    max_rounds = 120

[client]
    zoom= 6
    center= 1475042.8063459413,6077055.881901362
    ws_address = ws://127.0.0.1:9001
    debug = yes
    websocket = no

[chat]
    enabled = no
    toggle_on_enter = yes

    # flag | herald | iso
    country_style = flag

    msg_show_username = no
    msg_show_country = yes

    prof_show_username = yes
    prof_show_country = yes

[borders]
    enabled = no

[flags]
    isos = AA,UK,FR,HU,DE

    # for flags (with flags crafter):
    dim = 240,160
    spritesheet = yes

    # for heralds (if you use coats of arms collection):
    #dim = 512,512
    #spritesheet = no

    # for flags (each flag is its on image)
    #dim = 240,160
    #spritesheet = no

[rating]
    start_elo = 1100
    promo_elo = 1800
    ladder_elo = 2400
